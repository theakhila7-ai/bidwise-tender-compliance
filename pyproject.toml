[project]
name = "bidwise-tender-compliance"
version = "0.1.0"
description = "Tender compliance verification prototype"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
    "pypdf",
]

[tool.vercel]
entrypoint = "backend.main:app"
